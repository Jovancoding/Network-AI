# Python dependencies for Swarm Orchestrator Skill
#
# ZERO required third-party packages.
# All 6 scripts (blackboard.py, swarm_guard.py, check_permission.py,
# validate_token.py, revoke_token.py, context_manager.py) use Python standard library only:
#   argparse, json, os, re, sys, time, hashlib, uuid, datetime, pathlib,
#   typing, contextlib, cast, fcntl (Unix built-in, with file-lock fallback on Windows)
#
# No pip install is needed to run this skill.
#
# ── OPTIONAL (uncomment if needed) ──────────────────────────────────────────
# filelock>=3.0.0  # Cross-platform file locking (recommended for Windows production)
# mypy>=1.0.0      # Static type checking
# pytest>=7.0.0    # Running the test suite
